services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: saf_brain
      POSTGRES_USER: saf
      POSTGRES_PASSWORD: saf_local
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
